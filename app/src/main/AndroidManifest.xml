<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pbd.githubusers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic__launcher_github"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic__launcher_github_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUsers">
        <activity
            android:name=".FavoriteActivity"
            android:label="Favorites"
            android:exported="false" />
        <activity
            android:name=".DetailUserActivity"
            android:configChanges="orientation"
            android:exported="false"
            android:label="Detail User"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Github Users"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>